export const environment = {
  production: true,
  // For public beta, set browser localStorage override after deployment:
  // localStorage.setItem('cacaApiBaseUrl', 'https://YOUR-RAILWAY-BACKEND/api')
  // Later Step 30 can hardcode or inject this at build time.
  apiBaseUrl: ''
};
